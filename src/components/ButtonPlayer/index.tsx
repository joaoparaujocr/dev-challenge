import Player from "@/Icons/Player";
import Button from "./styles";

const ButtonPlayer = () => {
  return (
    <Button>
      <Player />
      <p>Assistir</p>
    </Button>
  );
};

export default ButtonPlayer;
